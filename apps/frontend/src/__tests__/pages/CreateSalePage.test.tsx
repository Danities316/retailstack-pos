import { render, screen, fireEvent, waitFor } from '@testing-library/react'
import { describe, it, expect, vi, beforeEach } from 'vitest'


describe('CreateSalePage (removed)', () => {
  it.skip('placeholder — CreateSalePage was removed in Sprint 2', () => {
    // See NewSalePage for the canonical POS flow and its tests
  });
});